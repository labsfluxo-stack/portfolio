.superpowers/
node_modules/
.next/
out/
next-env.d.ts
*.tsbuildinfo
.env*.local
test-results/
playwright-report/

# Cache local do wrangler (Worker da auditoria)
workers/**/.wrangler/

# Segredos locais do wrangler (chave do Groq). NUNCA versionar.
workers/**/.dev.vars
