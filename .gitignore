.superpowers/
node_modules/
.next/
out/
next-env.d.ts
*.tsbuildinfo
.env*.local
test-results/
playwright-report/

# Cache local do wrangler (Worker da auditoria)
workers/**/.wrangler/

# Segredos locais do wrangler (chave do Groq). NUNCA versionar.
workers/**/.dev.vars


# Capturas e scripts descartaveis das revisoes visuais.
# Regra AMPLA de proposito: a versao anterior listava prefixo por prefixo
# (nv-, av-, blog-) e dois arquivos novos (grafico-*.png) escaparam para um
# commit mesmo assim. Imagem de trabalho na RAIZ do repositorio nunca e
# artefato do site — o que o site publica vive em public/.
/*.png
/*.tmp.*
